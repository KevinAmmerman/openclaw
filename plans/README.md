# Implementation Plans

Generated by the improve skill on 2026-06-13. Execute in the order below.
Read the plan fully, preserve unrelated worktree changes, and run every
verification gate before marking it done.

## Execution order and status

| Plan | Title                                                     | Priority | Effort | Depends on | Status                                                                                                                      |
| ---- | --------------------------------------------------------- | -------- | ------ | ---------- | --------------------------------------------------------------------------------------------------------------------------- |
| 001  | Enforce terminal-only assistant delivery for noisy models | P1       | M      | none       | BLOCKED (source verified; local live canary cannot start because the 2026.4.27 fork bundles a plugin requiring >=2026.5.28) |

Status values: TODO, IN PROGRESS, DONE, BLOCKED, REJECTED.

## Dependency notes

- Plan 001 is self-contained.

## Findings considered and rejected

- Prompt rules in `AGENTS.md` or `SOUL.md`: rejected because they alter agent
  behavior and remain probabilistic.
- Expanding commentary regex/marker lists: rejected because GLM emits ordinary
  unphased assistant `text`; wording and language are unbounded.
- Telegram streaming changes: rejected as the primary fix because streaming is
  already off and final payload assembly can still include accumulated
  intermediate assistant text.
- Disabling model thinking as the only fix: rejected because the verified leak
  examples are normal `text` blocks on `toolUse` turns, not `thinking` blocks.
